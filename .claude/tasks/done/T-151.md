# T-151 | Make demo corridor-agnostic and update all documentation

- **Status:** done
- **Phase:** 1
- **Priority:** P1
- **Category:** docs, frontend
- **Effort:** L
- **Dependencies:** T-150
- **Branch:** feat/T-151-corridor-agnostic
- **Assignee:** Valerio Mellini
- **Notes:**
- **Section:** 1I. Validation and Cleanup

## Description

The demo is a generic trade corridor platform that is contextualized by a config file in `configs/`. It is NOT Vietnam-specific. All documentation and remaining hardcoded references must reflect this.

### Code fix: XmlViewer.jsx (line 211)

`VietnamContentPercent` is hardcoded in `client/src/components/XmlViewer.jsx`. This breaks for non-Vietnam corridors (e.g., ADAPT Africa). Replace with a config-driven or generic field name (e.g., `LocalContentPercent`) so the label works for any corridor.

### Documentation updates

All docs must be rewritten to describe a **generic trade corridor demo** that uses config files for corridor-specific context. Vietnam is one example corridor, not the default identity.

**README.md:**
- Title/description: generic (e.g., "Trade Corridor Demo Platform")
- Explain the config-driven architecture: `configs/vietnam-us.json`, `configs/adapt-africa.json`
- Show how to start with a specific config: `CONFIG_FILE=configs/adapt-africa.json npm run demo`
- Credentials section: explain they come from config, show example
- Remove Vietnam-specific framing from the main narrative

**DEMO_GUIDE.md:**
- Reframe as a generic walkthrough template
- Explain that orgs, documents, credentials come from the active config
- Keep Vietnam as the primary example but make clear it's one of many possible corridors
- Mermaid diagram: note it reflects the Vietnam corridor config

**.claude/CLAUDE.md:**
- Update project title and overview to reflect corridor-agnostic nature
- Keep technical details (ports, commands, structure) as-is

**docs/presentation-ux-recommendations.md:**
- Add a note at the top that this was written for the Vietnam corridor
- No need to rewrite entirely — it's a historical presentation guide

**package.json:**
- `name`: consider changing from `twin-vietnam-demo` to `trade-corridor-demo` or similar
- `description`: update to reflect generic nature

### Acceptance criteria

- `XmlViewer.jsx` renders local content percentage correctly for both Vietnam and ADAPT Africa configs
- README.md clearly describes a corridor-agnostic platform
- DEMO_GUIDE.md works as a template for any corridor
- CLAUDE.md project overview is updated
- `npm run build` succeeds
- Running with `CONFIG_FILE=configs/adapt-africa.json` shows no Vietnam-specific text in the UI
