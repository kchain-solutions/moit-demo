# T-150 | Rename all "tangle" references to "IOTA Mainnet" / "ledger"

- **Status:** done
- **Phase:** 1
- **Priority:** P1
- **Category:** frontend, server
- **Effort:** M
- **Dependencies:** none
- **Branch:** feat/T-150-rename-tangle-to-ledger
- **Assignee:** Valerio Mellini
- **Notes:**
- **Section:** 1I. Validation and Cleanup

## Description

Every reference to "tangle" in the codebase must be renamed. The DLT layer is IOTA Mainnet; internal code uses "ledger" as the generic term, and user-facing labels use "IOTA Mainnet" where appropriate.

### File renames

| Current | New |
|---------|-----|
| `data/tangle-alpha.json` | `data/ledger-alpha.json` |
| `data/tangle-beta.json` | `data/ledger-beta.json` |
| `client/src/components/TangleExplorer.jsx` | `client/src/components/LedgerExplorer.jsx` |

### Server changes (`server/index.js`)

- `TANGLE_FILE` → `LEDGER_FILE` (path constant, references `data/ledger-{id}.json`)
- `loadTangleLog()` → `loadLedgerLog()`
- `saveTangleLog()` → `saveLedgerLog()`
- `store.tangleLog` → `store.ledgerLog` (all occurrences)
- WebSocket message types: `TANGLE_UPDATE` → `LEDGER_UPDATE`, `TANGLE_ENTRY` → `LEDGER_ENTRY`
- API route: `/api/tangle` → `/api/ledger`
- All related console log messages and comments

### Client API (`client/src/utils/api.js`)

- `getTangle()` → `getLedger()`, endpoint `/api/tangle` → `/api/ledger`

### Client context (`client/src/context/NodeContext.jsx`)

- State: `tangleLog` / `setTangleLog` → `ledgerLog` / `setLedgerLog`
- WebSocket handler: `TANGLE_UPDATE` → `LEDGER_UPDATE`
- API call: `api.getTangle()` → `api.getLedger()`

### Client components

- **App.jsx:** nav item `id: 'tangle'` → `id: 'ledger'`; conditional render and import updated for `LedgerExplorer`
- **Dashboard.jsx:** destructure `ledgerLog` instead of `tangleLog`, update all references
- **TangleExplorer.jsx → LedgerExplorer.jsx:** rename function, update all `tangleLog` references
- **Network.jsx:** destructure `ledgerLog` instead of `tangleLog`, update display references

### UI labels

Where the UI currently shows "Tangle Explorer" or "Tangle", replace with "IOTA Mainnet" or "Ledger" as appropriate for the context (e.g., the Analytics page title, nav labels).

### Acceptance criteria

- `npm run build` succeeds with no warnings
- `npm run demo` starts correctly, ledger data loads from `data/ledger-*.json`
- Analytics page renders correctly with renamed component
- WebSocket sync between nodes works (ledger entries propagate)
- No remaining occurrences of "tangle" in code (documentation excluded from this task)
