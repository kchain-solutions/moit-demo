# T-161 | Extract shared frontend utilities and components

- **Status:** backlog
- **Phase:** 1
- **Priority:** P1
- **Category:** frontend
- **Effort:** S
- **Dependencies:** none
- **Branch:** feat/T-161-shared-frontend-utils
- **Assignee:**
- **Notes:** Blocks T-164 (modal standardization). Can be worked in parallel with T-160 (server track).
- **Section:** 1J. Code Quality and Refactoring

## Description

Extract duplicated utility functions and UI primitives from multiple component files into shared modules. Remove all duplicate definitions from source components.

### Duplicated code map (from audit)

| Function | Files | Notes |
|----------|-------|-------|
| `fmtValue()` | Dashboard.jsx (L11-23), Consignments.jsx (L7-19) | Identical copies |
| `fmtVal()` | TradeFinance.jsx (L8-12), Payments.jsx (L7-14) | Same logic, different name |
| `StatusPill` | Dashboard.jsx (L25-36), Consignments.jsx (L21-32) | Identical copies |
| `DocsPill` | Dashboard.jsx (L38-41), Consignments.jsx (L34-37) | Identical copies |
| `DOC_COLORS` | Dashboard.jsx (L43-50), Consignments.jsx (L39-46) | Identical copies |
| `DocTypeIcon` | Dashboard.jsx (L52-59), Consignments.jsx (L48-55) | Identical copies |

### Files to create

1. **`client/src/utils/format.js`**
   - `fmtValue(v)` - unified value formatter (merge fmtValue and fmtVal into one)
   - `fmtCurrency(amount, currency)` - if currency formatting diverges from value formatting

2. **`client/src/components/shared/Pills.jsx`**
   - `StatusPill` component
   - `DocsPill` component
   - `DocTypeIcon` component
   - `DOC_COLORS` constant

3. **`client/src/components/shared/Modal.jsx`**
   - Wrapper component using existing CSS `modal-bg` / `modal` classes
   - Props: `open`, `onClose`, `title`, `children`, `width` (optional)
   - Must preserve the mobile bottom-sheet behavior from the CSS class

### Files to update (remove duplicates)

- `client/src/components/Dashboard.jsx` - remove fmtValue, StatusPill, DocsPill, DOC_COLORS, DocTypeIcon; import from shared
- `client/src/components/Consignments.jsx` - same removals
- `client/src/components/TradeFinance.jsx` - remove fmtVal; import fmtValue from shared
- `client/src/components/Payments.jsx` - remove fmtVal; import fmtValue from shared

### Acceptance criteria

- No duplicate function definitions remain across component files
- All 4 affected components render identically to before
- `npm run build` succeeds with no warnings
- Shared modules have no circular dependencies
- Both corridor configs work correctly
