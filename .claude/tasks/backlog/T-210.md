# T-210 | Implement adapter auth routes

- **Status:** backlog
- **Phase:** 2
- **Priority:** P0
- **Category:** adapter
- **Effort:** S
- **Dependencies:** T-209
- **Branch:**
- **Assignee:** Valerio Mellini
- **Notes:** Simplified per ADR-001. No TWIN Node auth mapping needed. Demo login stays as-is. DID creation happens on org registration (T-211).
- **Section:** 2C. Real DID and Identity Integration

## Description

`POST /api/login`, `GET /api/node`. In `in-process` mode, auth stays identical to current behavior. Adapter `isReady()` status exposed via `GET /api/node`.

### Acceptance criteria

- Login works identically in both `simulated` and `in-process` modes
- Adapter initialization status exposed via `GET /api/node` response
